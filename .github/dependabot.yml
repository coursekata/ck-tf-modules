# Action SHAs only — terraform and pre-commit moved to Renovate, which cannot write workflow files.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
